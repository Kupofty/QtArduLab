# QtArduLab
A Qt-based GUI application designed to communicate with Arduino over serial. It allows sending and receiving messages, controlling the Arduino (e.g., LED), and viewing timestamped logs of incoming data.
